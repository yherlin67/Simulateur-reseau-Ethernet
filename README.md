# SAE2.3

## affichage.c
ensemble de fonctions utilitaires qui permettent d'afficher nos composants principaux du LAN (switch, stations, adresses ip...)

## index pour choisir l'envoi d'une trame depuis des stations (changer 0 en 1) + pas proposer les stations depuis lesquelles on ne peut rien envoyer car pas relisées à un switch 
## si l'utilisateur veut quitter si il tape autre chose que q on met un message d'erreur
## pour la table de commutation + l'envoi de trames il faut porposer à l'utilisteur en plus de quitter d'envoyer une autre trae ou de visualiser une autre table de commutation
## afficher le nombre de switchs max quand on veut voir la table de commutation.